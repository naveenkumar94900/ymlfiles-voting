apiVersion: v1
kind: Pod
metadata:
  name: voting-app-pod
  labels:
    name: voting-app-app
    app: voting-app
spec:
  containers:
    - name: voting-cont
      image: dockersamples/examplevotingapp_vote
      ports:
        - containerPort: 80

